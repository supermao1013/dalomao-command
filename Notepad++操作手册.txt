
1.在一列数据左边添加固定符号
(1)鼠标放在第一行第一个字符前面，然后：编辑-列块编辑-插入文本中输入字符
(2)【优先】行首替换法：
	a.Ctrl+H弹窗替换窗口
	b.查找目标为：^（因为^表示行首）,替换目标为目标字符，查找模式设置为正则表达式
	
2.在一列数据右边添加固定符号
(1)如果每一行的字符串长度一致，则鼠标放在第一行最后一个字符前面：编辑-列块编辑-插入文本中输入字符
(2)【优先】如果每一行的字符串长度不一致，不能采用方式(1)，采取：
	a.Ctrl+H弹窗替换窗口
	b.查找目标为：$（因为$表示行尾）,替换目标为目标字符，查找模式设置为正则表达式